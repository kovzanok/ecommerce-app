export type CreateApiData = {
  email: string;
  password: string;
};

export type PageLink = {
  to: string;
  name: string;
  description: string;
};

export type Country = {
  value: string;
  label: string;
};
